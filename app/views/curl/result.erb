<div class="stage">
  <div class="stage-heading">
    <div class="stage-title">Curl API Results</div>
    <div class="stage-subtitle">See the results generated by the request below as well as the curl command that could be used to make the call as well.</div>
  </div>
</div>

<div class='panel p-lg'>

  <div>
    <h3>Curl Command <button class="btn btn-copy btn-info pull-right" data-clipboard-target=".curl-cmd"><i class="fa fa-copy"></i> Copy Curl</button></h3>
    <pre class="curl-cmd"><%= curl_call %></pre>
  </div>

  <div>
    <h3>Results</h3>
    <pre class="call-results"><%= JSON.pretty_generate(result) %></pre>
  </div>

</div>